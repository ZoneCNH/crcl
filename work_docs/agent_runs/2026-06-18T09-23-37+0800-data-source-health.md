# CRCL 数据源可获取性检查

检查时间：2026-06-18 09:23 CST

## 结论

当前未解除的数据获取缺口只有 1 个：`P1_EXCHANGE_USDC_BALANCES`，来源为 CoinGlass Exchange Balance List，需要 `COINGLASS_API_KEY`。

全量采集批次 `collect-1781745528059560000-34435` 结果为 `22 ok / 1 warn`。随后用更长超时复测事件源批次 `collect-1781745690972631000-44103`，FinCEN 页面恢复为 `ok 200`，因此不再作为当前阻断项。

## 当前未获取

| 优先级 | 指标 | 来源 | 当前结果 | 解除条件 |
| --- | --- | --- | --- | --- |
| P1 | `P1_EXCHANGE_USDC_BALANCES` | `https://open-api-v4.coinglass.com/api/exchange/balance/list?symbol=USDC` | HTTP 200，但响应体为 `{"code":"401","msg":"API key missing."}` | 配置 `COINGLASS_API_KEY` 后重跑 market/all 采集 |

## 已复核但不阻断

| 来源 | 默认全量采集结果 | 复测结果 | 判断 |
| --- | --- | --- | --- |
| FinCEN news room | `network_error`，默认 20 秒超时边界附近失败 | `--timeout-secs 45 collect --source events` 后 `ok 200` | 网站可达，建议监管页采集使用更长超时，避免误报 |

## 数据质量判断

data-quality 当前不被 P0 网站阻断。CoinGlass 是 P1 交易所 USDC 余额缺口，会降低周度竞争/流动性判断置信度，但不阻断日监核心 P0 数据。

## 执行证据

- `cargo run --release -- collect --source all`
  - batch：`collect-1781745528059560000-34435`
  - started：`2026-06-18T01:18:48.059601+00:00`
  - finished：`2026-06-18T01:20:15.549491+00:00`
  - result：`22 ok / 1 warn`
- `cargo run --release -- --timeout-secs 45 collect --source events`
  - batch：`collect-1781745690972631000-44103`
  - result：`2 ok / 0 warn`
- `cargo run --release -- missing`
  - only remaining item：`P1_EXCHANGE_USDC_BALANCES`

## 下一步

1. 配置 `COINGLASS_API_KEY`，然后重跑 `cargo run --release -- collect --source market` 或 `cargo run --release -- collect --source all`。
2. 若 cron 继续使用默认 20 秒超时，建议把监管页任务的 `--timeout-secs` 提高到 45 或 60，避免 FinCEN 慢响应造成假阳性。
