# CRCL monitoring guard final

结论：紧急。

data-quality 是否阻断：不硬阻断，但降置信度。本轮阻断项不是 data-quality，而是 P0 技术状态异常：Base status 返回 `Partially Degraded Service`。

collector_run：batch_id=`decision-pack-1781862593804623000-72021`；command=`decision-pack`；selector=`all`；started_at=`2026-06-19T09:49:53.804651+00:00`；finished_at=`2026-06-19T09:51:22.927822+00:00`；generated_at=`2026-06-19T09:51:22.952025+00:00`；ok_sources=22；warn_sources=1。source_runs=38，状态分布：37 ok / 1 decode_error。监控只读命令已执行：`cargo run --release -- --database data/crcl_research.sqlite agent-context --profile monitor --format json --no-collect`。

失败源：唯一失败源为 `CoinGlass frontend Exchange Balance List: USDC`，status=`decode_error`，HTTP 200，fetched_at=`2026-06-19T09:50:07.257781+00:00`，source_url=`https://capi.coinglass.com/api/exchange/chain/v3/balance/list?symbol=USDC&t=1781862606765`，error=`unsupported CoinGlass frontend encryption v=1`。这是二级平台 P1 交易所余额源失败，不是官方监管源或 P0 官方源失败。

过期指标：`P0_CIRCLE_RESERVE_FUND_7D_YIELD` = 3.57%，observed_at=`2026-05-29`，source=`SEC EDGAR filing: BlackRock Funds N-MFP3`，source_url=`https://www.sec.gov/Archives/edgar/data/844779/000119312526258597/xslN-MFP3_X01/primary_doc.xml`。这是 SEC N-MFP3 月度口径，监控上标记 stale，需要补 BlackRock/USDXX 日度人工交叉验证，但本轮未构成储备覆盖异常。

P0 / 紧急事件：Base status 为 `Partially Degraded Service`，observed_at=`2026-06-19T01:24:49.530Z`，source_url=`https://status.base.org/api/v2/status.json`，按监控纪律作为 P0 技术状态异常升级为紧急。未发现 USDC/USD 折价触发：`P0_USDC_USD_PRICE` = 0.999826，observed_at=`2026-06-19`，source=`CoinGecko`。Circle status 为 `All Systems Operational`，observed_at=`2026-06-19T08:44:32.211Z`；Solana 为 `All Systems Operational`，observed_at=`2026-06-19T08:04:10.660Z`；Ethereum 为 `Operational`，observed_at=`2026-06-19T09:51:22.926285+00:00`。Circle Transparency 显示 total reserves 75.10B USD、USDC circulation 74.86B USD，observed_at=`2026-06-15`，source_url=`https://www.circle.com/transparency`，未显示储备覆盖异常。USDC 7D net issuance -1.05B USD、30D net issuance -1.96B USD，observed_at=`2026-06-15`，作为观察项保留。

missing_info：P1 缺口 3 项，均来自 CoinGlass 解密失败：`P1_EXCHANGE_USDC_BALANCES`、`P1_EXCHANGE_USDC_BALANCE_30D_CHANGE`、`P1_EXCHANGE_USDC_TOP3_CONCENTRATION`。未见 P0 missing_info。

补采 / 人工核验动作：1. 等待或修复 CoinGlass frontend encryption v=1 解密适配，补采命令：`cargo run --release -- collect --source exchange-usdc-balances`；若已配置 Open API key，先设置 `COINGLASS_API_KEY` 后重跑同命令。2. 人工打开 `https://status.base.org/api/v2/status.json` 或 Base status 页面确认 degraded 组件、开始时间和恢复公告。3. 对 `P0_CIRCLE_RESERVE_FUND_7D_YIELD` 做 BlackRock/USDXX 基金页面人工交叉验证；机器侧可重跑 `cargo run --release -- collect --source rates`，但应在下一批证据中执行，避免污染本批口径。

解除条件：Base status 恢复 `All Systems Operational` 或等价正常状态，并在下一轮同 workflow evidence packet 中落库；CoinGlass P1 三项 missing_items 清零或有可接受替代源；`P0_CIRCLE_RESERVE_FUND_7D_YIELD` 完成最新月度 SEC filing 或 BlackRock/USDXX 日度人工交叉验证；source_runs 中官方与关键状态源保持 HTTP 200 且无 `SOURCE_BLOCKED` / `SOURCE_UNREACHABLE`。

下次自动检查触发：下一轮 `monitoring` cron / `codex exec`；若 Base status 仍为 degraded、Circle status 异常、USDC/USD 偏离超过框架阈值、出现 P0 missing_info、SEC / Circle / OCC / Treasury / FinCEN / OFAC / FDIC / CFTC / Congress 任一关键官方源失败，应继续紧急；若仅剩 CoinGlass P1 缺口，则降为警戒。

已保存路径：`work_docs/monitoring/2026-06-19T09-51-22-952025+00-00-monitoring-guard-final.md`
